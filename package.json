{
  "name": "synchro-hrm-mailbox-ingestion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "prestart": "npm run build",
    "start": "node dist/src/index.js",
    "test": "npm run build && node dist/scripts/run-tests.js",
    "fixtures": "npm run build && node dist/scripts/generate-parser-fixtures.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "node-cron": "^4.2.1",
    "pdfjs-dist": "^4.10.38",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.8",
    "tsx": "^4.20.6",
    "typescript": "^5.8.3"
  }
}
